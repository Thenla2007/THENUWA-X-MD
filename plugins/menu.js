const { cmd, commands } = require("../command");
const config = require("../config");

// ==========================================
// 1. ප්‍රධාන MENU විධානය (.menu ගැසූ විට)
// ==========================================
cmd(
  {
    pattern: "menu",
    alias: ["getmenu"],
    react: "📔",
    desc: "Get simple text-based category menu",
    category: "main",
    filename: __filename,
  },

  async (robin, mek, m, { from, reply }) => {
    try {
      // ලස්සන Text Menu එකක් සාදා ගැනීම (පහසුවෙන් වැඩ කිරීමට තිතක් සමඟ)
      let menuText = `👋 *Hello welcome to DENETH-MD*\n\n` +
                     `> *Generated by DENETH-MD* 💢\n\n` +
                     `පහත දැක්වෙන විධානයන් Copy කර Reply කරන්න. 👇\n\n` +
                     `🔹 *.1*  - ALL COMMANDS 📊\n` +
                     `🔹 *.2*  - DOWNLOAD MENU ⬇️\n` +
                     `🔹 *.3*  - GROUP MENU 👥\n` +
                     `🔹 *.4*  - OWNER MENU 👑\n` +
                     `🔹 *.5*  - CONVERT MENU 🎨\n\n` +
                     `© Powered By Deneth MD`;

      // ඉමේජ් එකත් සමඟ Text එක යැවීම
      return await robin.sendMessage(from, { 
          image: { url: "https://ibb.co" }, 
          caption: menuText 
      }, { quoted: mek });

    } catch (e) {
      console.error("MENU ERROR:", e);
      reply(`❌ Menu Error\n\n${e.message || e}`);
    }
  }
);

// ==========================================
// 2. අංක විධානයන් ක්‍රියාත්මක වන කොටස (.1, .2, .3 වැනි)
// ==========================================

// 📊 ALL COMMANDS (.1)
cmd({
    pattern: "1",
    dontAddCommandList: true,
    filename: __filename
}, async (robin, mek, m, { from }) => {
    let menuMessage = `📊 *DENETH-MD ALL COMMANDS LIST* 📊\n\n`;
    commands.map((cmd) => {
        if (!cmd.dontAddCommandList && cmd.pattern) {
            menuMessage += `▪️ *Command:* .${cmd.pattern}\n▪️ *Desc:* ${cmd.desc || 'No description'}\n\n`;
        }
    });
    return await robin.sendMessage(from, { text: menuMessage }, { quoted: mek });
});

// ⬇️ DOWNLOAD MENU (.2)
cmd({
    pattern: "2",
    dontAddCommandList: true,
    filename: __filename
}, async (robin, mek, m, { from }) => {
    let menuMessage = `✨ *DENETH-MD DOWNLOAD MENU* ✨\n\n`;
    let found = false;
    commands.map((cmd) => {
        if (!cmd.dontAddCommandList && cmd.pattern && cmd.category && cmd.category.toLowerCase() === "download") {
            menuMessage += `▪️ *Command:* .${cmd.pattern}\n▪️ *Desc:* ${cmd.desc || 'No description'}\n\n`;
            found = true;
        }
    });
    if(!found) menuMessage += `▪️ .song (Audio Download)\n▪️ .video (Video Download)\n▪️ .fb (Facebook Download)\n\n`;
    menuMessage += `> *© Powered by DENETH-MD*`;
    return await robin.sendMessage(from, { text: menuMessage }, { quoted: mek });
});

// 👥 GROUP MENU (.3)
cmd({
    pattern: "3",
    dontAddCommandList: true,
    filename: __filename
}, async (robin, mek, m, { from }) => {
    let menuMessage = `✨ *DENETH-MD GROUP MENU* ✨\n\n`;
    let found = false;
    commands.map((cmd) => {
        if (!cmd.dontAddCommandList && cmd.pattern && cmd.category && cmd.category.toLowerCase() === "group") {
            menuMessage += `▪️ *Command:* .${cmd.pattern}\n▪️ *Desc:* ${cmd.desc || 'No description'}\n\n`;
            found = true;
        }
    });
    if(!found) menuMessage += `▪️ .kick (Remove Member)\n▪️ .add (Add Member)\n▪️ .promote (Admin Rule)\n\n`;
    menuMessage += `> *© Powered by DENETH-MD*`;
    return await robin.sendMessage(from, { text: menuMessage }, { { quoted: mek } });
});

// 👑 OWNER MENU (.4)
cmd({
    pattern: "4",
    dontAddCommandList: true,
    filename: __filename
}, async (robin, mek, m, { from }) => {
    let menuMessage = `✨ *DENETH-MD OWNER MENU* ✨\n\n`;
    let found = false;
    commands.map((cmd) => {
        if (!cmd.dontAddCommandList && cmd.pattern && cmd.category && cmd.category.toLowerCase() === "owner") {
            menuMessage += `▪️ *Command:* .${cmd.pattern}\n▪️ *Desc:* ${cmd.desc || 'No description'}\n\n`;
            found = true;
        }
    });
    if(!found) menuMessage += `▪️ .restart (Restart Bot)\n▪️ .block (Block User)\n\n`;
    menuMessage += `> *© Powered by DENETH-MD*`;
    return await robin.sendMessage(from, { text: menuMessage }, { quoted: mek });
});

// 🎨 CONVERT MENU (.5)
cmd({
    pattern: "5",
    dontAddCommandList: true,
    filename: __filename
}, async (robin, mek, m, { from }) => {
    let menuMessage = `✨ *DENETH-MD CONVERT MENU* ✨\n\n`;
    let found = false;
    commands.map((cmd) => {
        if (!cmd.dontAddCommandList && cmd.pattern && cmd.category && cmd.category.toLowerCase() === "convert") {
            menuMessage += `▪️ *Command:* .${cmd.pattern}\n▪️ *Desc:* ${cmd.desc || 'No description'}\n\n`;
            found = true;
        }
    });
    if(!found) menuMessage += `▪️ .sticker (Image to Sticker)\n▪️ .convert (Sticker to Image)\n\n`;
    menuMessage += `> *© Powered by DENETH-MD*`;
    return await robin.sendMessage(from, { text: menuMessage }, { quoted: mek });
});
